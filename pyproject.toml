[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Portal da Transparência"
authors = [{name = "GEDAD", email = "gedad@cge.sc.gov.br"}]
dynamic = ["version", "description"]
